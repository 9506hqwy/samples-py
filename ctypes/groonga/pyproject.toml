[project]
name = "ctypes-groonga"
version = "0.1.0"
authors = [{ name = "9506hqwy" }]
description = "ctypes groonga sample."
readme = "README.md"
requires-python = ">=3.12"

[build-system]
requires = ["uv_build>=0.11.28,<0.12.0"]
build-backend = "uv_build"
